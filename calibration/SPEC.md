# 📋 校準資料集規格書（CALIBRATION SPEC）

> 本檔是校準資料集的完整規格：欄位字典、有效性規則、預登記分析協議、
> 重校程序與 artifact 案底。目的：三個月後的維護者或任何外部研究者，
> 不需要任何對話上下文即可正確使用這批數據做 alpha 研究與等級重校。
> 隨每次出口同步到公開庫 `calibration/SPEC.md`。schema_version = 2。

## 0. 主假說與 Alpha 策略（整個資料集為此而存在）

**主假說 H**：假說樹的判定變化走在股價前面——純降級股其後一週跑輸全池
中位、純升級股跑贏；等級愈重、降級愈齊，效應愈強。

**Alpha 策略讀數**：做多純升級組、做空純降級組（組內等權）。此組合的
市場中性回報＝

```
策略回報（strategy return）＝升級籃超額均值 − 降級籃超額均值
    某週缺一邊 ⇒ 該邊以 0 計（缺邊以全池對沖）——單邊週照樣計分：
    僅有降級組時，策略回報＝−降級組超額（做空降級、對沖全池）
組差（spread）＝雙邊週之策略回報（兩邊皆有時兩者相等）
加權版＝同式，但以 |訊號分| 為權
訊號分＝Σ±重要度權重（●●●●● 極高 2.5／●●●● 高 1.6／●●● 中 0.9／
        ●● 低 0.4／● 極低 0.15；升級＋、降級−）
    同葉同週淨額化（v3.8）：同一片葉同一週多次轉變只計一次（首 from
    → 末 to 之淨方向；淨額為零不計）——一葉一週一票，杜絕同葉連跳
    重複計數（07-19 週 TSM C3 案例：−10.0 → −7.5）
```

大盤升跌與板塊順逆風在策略回報中互相抵銷——**策略回報序列持續 >0 即
策略有 alpha**，單邊命中率只是輔助讀數。超額＝個股一週回報−全池中位
（口徑同 §5.2）。

**持有期**：非固定一週。倉位持有至 (a) 該判定被下一次轉變推翻（state
翻轉即平倉／反手），或 (b) H\* 週上限——H\* 由 H-horizon 之
`excess_h1..h8` 曲線見頂位置決定，樣本累積後成文。每週對答案窗口只是
**最小追蹤單位**（每個名單 cohort 之 h1 讀數），多週延續效應由校準
曲線自動累積（同一 cohort 之 h2、h3……逐週補齊）。

重要度權重是**變數而非常數**：隨校準樣本累積按 §6 以 Bayesian 信度
混合重估，上列為現行值；事實源＝`_lib/tree_quant.py` 之 `IMPACT_GRADES`，
週報訊號分／加權組差／方法腳註皆即時讀取該處，重校後自動跟隨。

**重要度的儲存鍵與顯示名（2026-08-02 改名，鍵與數值一律不變）**

| JSON 內的 `impact_grade` | 顯示 | 權重 |
|---|---|---|
| `致命` | ●●●●● 極高 | 2.5 |
| `重創` | ●●●● 高 | 1.6 |
| `明顯受損` | ●●● 中 | 0.9 |
| `輕微` | ●● 低 | 0.4 |
| `邊緣` | ● 極低 | 0.15 |

儲存鍵是設樹時的定義語（「此分支若被推翻，主張受到何等打擊」），全部帶
傷害色彩；但這個量方向中性——同一片高重要度的葉升級時，讀出「↑致命」
自相矛盾，讀者更會誤讀成「這則消息有多嚴重」。故顯示層改用序數階梯。
本文其餘各節用顯示名；`events.csv`／`structure.json` 內仍是儲存鍵，
自行重算者請照 JSON 取值。變更日之前的變更記錄（§ 變更史）保留原文，
不追改。

**檢驗機制**：每週日名單先公開（Slack ＋ `verdict_watch.jsonl`
append-only 帳本），下週日對答案；逐週 `spread_pp`／`wspread_pp` 落賬；
累積 ≥30 個獨立降級樣本後跑正式 permutation 檢定（§5.4）。mixed 週
（同股同週有升有降）依 H-purity 不入比分；recode 與模型遷移事件於
源頭剔除（§4.1、§7）。實驗規則全文見公開庫
`portfolio/PUBLIC_EXPERIMENT_20260722.md`。

## 1. 資料層級

```
verdict_history（假說 JSON，事實源）
  → events.csv    一行一個判定轉變事件（raw ＋ enrichment）
  → clusters.csv  一行一個 ticker×事件週 cluster（廣度＋cascade）
  → CALIBRATION.md 結果表（全部由 analysis.py 生成，人手不落數字）
```

生成鏈：`tree_quant.py cmd_calib_table`（raw）→ `analysis.py`（enrichment
＋統計）。兩支腳本隨出口發布，外部跑同一份 code 得同一份數。

## 2. events.csv 欄位字典

| 欄 | 定義 | 空值語義 |
|---|---|---|
| `schema_version` | 本表 schema 版本（現為 2） | 不空 |
| `event_id` | `ticker:hypothesis:date`，穩定主鍵 | 不空 |
| `ticker` / `hypothesis` / `branch` | 樹、子假說 ID、所屬分支 ID | branch 空＝無法歸屬 |
| `impact_grade` | 分支重要度（§1.5 封閉枚舉，值為儲存鍵：致命/重創/明顯受損/輕微/邊緣，依次顯示為 極高/高/中/低/極低） | 空＝未設 |
| `grade_implied_from_fib` | True＝重要度由舊制 Fibonacci 權重遷移映射推得，非人工判定 | — |
| `branch_weight` | 分支 log-odds 合成權重（致命2.5/重創1.6/明顯受損0.9/輕微0.4/邊緣0.15） | — |
| `is_necessity_leaf` | 該假說是否分支之必要葉（證偽即殺支） | — |
| `model` | 判讀模型：entry 有 stamp 用 stamp（2026-07-22 起 apply-patch 寫入）；無則按日期推斷年代（<07-16 grok-4.5／07-16–17 deepseek-v3.2／≥07-18 deepseek-v4-pro） | 不空 |
| `model_transition` | True＝事件屬模型切換首個判讀日（07-16/17/18）的重新評分潮，非市場事件；統計表剔除、原始行保留 | — |
| `trigger` | 觸發源：`weekly`＝週六掃描；`earnings`＝財報後裁定（每日 cron 偵測業績剛公布，`--earnings-mode` 以發布材料重審）。兩層統計性質不同，分析須分層（§5.6 H-trigger） | 空＝2026-07-23 stamp 前 legacy，觸發源不可考 |
| `tree_age_days` | 事件日距該樹（全部假說）首個非 recode 判定事件的日數（研究協變量） | 空＝日期缺失 |
| `vocab_migration` | True＝事件 from/to 涉舊 3 值詞彙，或落於該樹詞彙遷移日 −1..+7 日窗口（§7：05-12..15 成批 remap ＋其後首個判讀週重新評分潮，一次性系統事件）；統計表剔除、原始行保留 | — |
| `price_era` | 對齊行價格年代：`hist_backfill`＝歷史回補 sidecar／`live`＝實時快照；統計分層呈報（§8.6） | 空＝對齊無效 |
| `date` / `from` / `to` | 事件日、轉變前後判定（六值枚舉） | — |
| `score_delta` | 判定分數差（✅+2/🟢+1/⚪0/🟡−1/🟠−2/🔴−3） | 空＝legacy 判定無法映射 |
| `direction` | downgrade / upgrade / lateral（按 score_delta 符號） | — |
| `next_transition_days` | 同一假說下一次非 recode 轉變距本事件日數＝本 state 實際壽命 | 空＝至今未再轉變（右刪失） |
| `days_since_prev_transition` | 距上一次轉變日數（轉變頻密度） | 空＝首次轉變 |
| `gap_days` | 事件日至最近一個不早於它的價格行的日數 | 空＝無價格行 |
| `row_date` / `conviction_at` / `price_at` | 對齊行（僅 `gap_days`≤7 時填）：日期、當時 H0 信念、快照價 | 空＝對齊無效 |
| `conviction_delta_1w` | 事件後一行 H0 信念變化（信念反應速度） | 空＝無下一行 |
| `fwd_h1..fwd_h8` | 對齊行起 +1..+8 行累計原始回報（每行≈1 週） | 空＝歷史不足或 leg 無效 |
| `pool_h1..pool_h8` | 同期全池（全 72 樹）同 leg 回報中位數 | 空＝該期無池數據 |
| `excess_h1..excess_h8` | `fwd_h − pool_h`（超額回報，**分析主口徑**） | 空＝任一輸入空 |
| `cluster_id` / `cluster_size` | `ticker@ISO週`；該 cluster 事件總數 | 不空 |

## 3. clusters.csv 欄位字典

| 欄 | 定義 |
|---|---|
| `cluster_id` / `ticker` / `first_event` / `last_event` | ticker×ISO 事件週；首末事件日 |
| `mix_type` | 訊號純度：down_only（純降級週）／mixed（同週有升有降，訊號互相抵銷）／up_only／lateral_only |
| `n_events` / `n_hypotheses` / `n_branches` | 廣度：事件數、涉及葉數、涉及分支數 |
| `n_down` / `n_up` / `n_lateral` | 方向分解 |
| `sum_score_delta` | 淨分數變化（正=淨升級） |
| `grade_weighted_delta` | Σ score_delta × 等級權重（衝擊加權淨變化） |
| `any_necessity_down` | 該週是否有必要葉降級 |
| `aligned` | 是否有成員事件通過價格對齊 |
| `fut_transitions_28d` / `fut_downgrades_28d` / `fut_upgrades_28d` | cascade：末事件日後 28 日內同 ticker 再現轉變/降級/升級次數 |
| `excess_h1..excess_h8` | cluster 層超額回報（對齊成員事件均值） |
| `next_state_change_days` | 該 ticker 下一次升／降級事件（不論任何旗）距 cluster 最後事件日數——state 持有規則（v3.4）用：+h 週格要求 ≥7h，否則窗口已跨入下一個 state，剔除 |

## 4. 有效性規則（引擎層執行，非事後篩選）

1. **recode 剔除**：`recode: true` 之轉變屬方法論重新編碼，非市場事件，源頭不入樣本。
2. **事件對齊**：事件日 7 日內無價格行 ⇒ 無「事件時價格」可言，對齊欄與
   前向回報一律留空（`gap_days` 照記供審計）。
3. **leg 有效性**：leg 起訖兩行 `snapshot_price_date` 相同 ⇒ 期間無重新
   標價，0% 屬 artifact，不出數。池基準 leg 同規則。

## 5. 預登記分析協議（先寫規則，後見數據）

1. **分析單位＝cluster**（ticker×事件週）。同週同股多事件共享同一段回報，
   事件層均值會重覆計數（案例：2026-07-10 ONDS 四葉齊降共享同一段 −10.1%）。
2. **主口徑＝超額回報**（扣同期全池中位）。原始回報只作參考表。
3. **Horizon family 固定 +1..+8 週，永遠呈報全條曲線**，不得只引用單一
   窗口（防事後挑選）。曲線右端隨追蹤歷史自然填滿。
4. **顯著性＝cluster 層單尾 permutation 檢定**（固定種子、20,000 次重抽），
   p 值照報，不因結果不好看而省略。
5. **state 壽命過濾**（嚴格讀法）：h×7 > `next_transition_days` 的格已跨入
   下一個 state，混雜；嚴格分析應剔除。
6. **預登記假說**：
   - **H-grade**：降級後超額回報為負，負向幅度隨重要度遞增。
   - **H-breadth**：同週降級廣度（`n_down`、`grade_weighted_delta`、
     `any_necessity_down`）愈大，未來 28 日 cascade 機率愈高、超額回報愈差。
   - **H-purity**（2026-07-22 增補）：逐隻股票逐週看淨方向——同週有升有降
     （mixed）＝訊號互相抵銷，不應與純降級週（down_only）混計；預期
     純降級週回報差於混合週，且純降級多葉週差於單葉週。**誠實聲明**：
     此假說由維護者檢視首批 19 個對齊事件（ACN 混合週 +4.6% 異常）後
     提出，對該批樣本屬 in-sample 探索——假說由數據啟發，不能用同一批
     數據證明自己；僅對其後累積的新樣本具預登記檢定效力。純度口徑的
     permutation 檢定（down_only vs up_only＋lateral_only，mixed 剔除）
     與原始口徑並列呈報。
   - **H-trigger**（2026-07-23 增補，先寫規則後見數據）：判定轉變按觸發源
     分兩層——`weekly`（週六掃描：判讀模型從一週新聞流中發現的增量資訊）
     與 `earnings`（財報後裁定：業績公布翌日以發布材料重審）。財報後層
     的股價已於公布時即時反應大半，其後一週超額回報測的是**財報後漂移**
     （post-earnings drift）而非純資訊發現；預期：(a) 兩層的降級後超額
     回報同號（漂移文獻支持財報後仍沿意外方向延續），但 (b) 週掃描層
     的幅度更大（財報層的資訊已部分入價）。兩層永遠分開呈報，不合併
     檢定；主組差照計全樣本，觸發源作分層輔助讀數，累積 ≥30 個獨立
     cluster／層後分層 permutation。stamp 前 legacy 事件（`trigger` 空）
     不入分層檢定。**財報層操作規則**（2026-07-23 增補）：(a) 財報後裁定
     產生判定轉變時，事件**當日即時公開**於實驗頻道（逐葉明細＋訊號分＋
     事件日收盤價）——「先公開後對答案」之日內版；(b) 其**計分基準＝事件
     日收盤**（earnings-mode 當日快照價），非週日名單日收盤——事件先行
     兩三日而名單日才開錶，會漏計（或倒計）事件後最烈的一段；(c) 事件照隨
     週日名單落賬（`earn_base`／`earn_base_date` 欄），對答案時由基準價
     開錶，池中位仍為週窗口徑——此窗口差異屬財報層固有，兩層分開呈報
     即為此故。
   - **H-horizon**（2026-07-23 增補）：效應的**持續期**本身是研究對象——
     判定轉變的效應並非一週內完結。預期：(a) 降級後累計超額回報於 +1..+4
     週持續走闊（漂移延續），不出現一週內完全均值回歸；(b) 持續期與
     state 壽命（`next_transition_days`）相關——判定未被下一次轉變推翻
     期間，效應持續；state 愈長命，可收割的窗口愈長。讀數＝
     `excess_h1..h8` 全條曲線（§5.3 已強制全曲線呈報，禁止單點引用）；
     嚴格讀法依 §5.5 剔除已跨入下一 state 的格。此假說決定策略的
     **持倉期參數**：若曲線於 h2–h3 見頂，正式實驗的一週對答案窗口
     之外應另設多週追蹤讀數。
   - **H-spread**（2026-07-23 增補，主策略讀數；同日修訂為策略回報口徑）：
     每週「策略回報」（§0：升級籃超額−降級籃超額，缺邊以 0 計——單邊週
     照計分）之時間序列均值 >0；加權版（|訊號分| 為權）同號且幅度不小於
     等權版——若加權版明顯弱於等權版，即「等級愈重效應愈強」不成立，
     IMPACT_GRADES 應依 §6 重校。逐週 `strat_pp`／`wstrat_pp`（及雙邊週
     之 `spread_pp`／`wspread_pp`）存於 `portfolio/verdict_watch.jsonl`
     之 review 欄。
   - **H-age（論點時效；維護者 2026-07-24 提出）**：樹的論點是有時效的
     ——分支與證偽條件寫於建樹一刻，隨時間過去，論點對「當下」的解釋力
     可能衰減；故判定轉變的訊號強度可能隨**樹齡**改變。方向兩說（老樹
     轉變可能因論點過期而失效，亦可能因倖存篩選而更準），預登記為
     **雙尾**檢定，不預設方向。
     - 變量：`tree_age_days`（events.csv，v3.3 起）；桶＝≤30／31–60／
       >60 日。讀數＝降級事件超額 `h1/h2/h4`（依 §5.5 state 持有規則）
       ＋負超額比例，逐桶呈報；樣本以獨立 cluster 計。
     - **識別警示（誠實聲明）**：現階段 37/53 棵樹同一週出生，樹齡與
       日曆週近乎完全共線（如 >60 日桶全部來自 07-11 名單週）——當前
       數據**不可識別**樹齡效應。分離依賴六月後 cohort（16 棵，出生日
       分散）累積事件；首讀門檻＝每桶 ≥10 個獨立 cluster。
     - 第二層（已設計、未實施）：**條件時效**——以 §8 結構化 conditions
       之 deadline（`due` 日期）距事件日數、過期條件比例，直接量度
       「論點過咗保質期」；較樹齡更貼近機制，待 conditions 覆蓋全艦隊
       後實施。
     - 行動鏈：若 H-age 確認（雙尾 |t|≥3、桶樣本達標），訊號分加樹齡
       衰減因子 λ(age)——函數形式須先預登記，λ 常數依 §6 同式信度混合
       估計；先入內部模擬對照，後方可入 live sizing。
   - **H-priced-in（市場先行阻尼；維護者 2026-07-25 提出）**：判定轉變
     若在**股價已沿訊號方向大幅移動之後**才出現（judge 追認既成事實），
     其預測力應顯著較弱——其後一週傾向均值回歸而非延續。
     - 變量：事前一週超額（名單前一週該股回報 − 事前池中位；事件層對應
       `excess_pre`）。預期：降級訊號中，事前超額深負者（候選門檻
       ≤−8pp）命中率顯著低於其餘；升級側鏡像。
     - **啟發樣本誠實聲明**：本假說由 07-19 週檢視啟發——該週事前超額
       ≤−8pp 之沽空訊號 4 隻（IBM／ALAB／MRVL／COHR）全數反彈落空，
       其餘大致命中；此為 in-sample 觀察，門檻與折扣率不得由該週寫死。
     - 行動鏈（v3.9 改寫；原為連續阻尼 λ(pre)，已撤回）：
       1. 分桶檢驗**兩個方向**——2026-07-26 實測顯示升級側同受反轉效應
          影響且更甚（事前 −11.0% vs 降級 −5.9%），只檢降級側會把市場
          反轉誤讀為「降級訊號較弱」。
       2. 計分板並排報「控制事前走勢後」之策略回報，主讀數不變。
       3. 累積 ≥30 個獨立樣本後，由分桶表估**單一門檻**，預登記後啟用
          二元 gate：|事前超額| 超過門檻且與訊號同向者標記「已反應」，
          如實入帳但不入籃。
       撤回連續阻尼之理由：λ(pre) 需同時估門檻與折扣率兩個參數，現有樣本
       估不出；且阻尼只改注碼不改方向，而若移動已完全反應，正確處置是
       不進場而非小注進場。二元 gate 只需一個參數，失敗後果亦較輕
       （「不入籃」而非「注碼算錯」）。
   - cascade 解讀警示：本管道每週重審同批假說，同一單新聞可連續兩週觸發
     轉變，「再有轉變」部分是管道自身持續反應；一律對比無降級週基準行。

## 6. 等級重校程序（IMPACT_GRADES 何時改、如何改）

1. **門檻**：某等級累積 ≥30 個獨立降級 cluster（aligned）方可重估。
2. **估計**：以該等級 cluster 層 `excess_h1..h4` 均值曲線推算實證衝擊，
   與現行常數（致命2.5/重創1.6/明顯受損0.9/輕微0.4/邊緣0.15）比較。
3. **混合**：新常數＝信度加權（§5.6 同式：Z=m/(m+k)，m＝獨立 cluster 數）
   之實證值與專家先驗混合——樣本愈多，數據的決定權愈大。
4. **紀錄**：任何常數修改必須在 RELEASES.md 開新版本、附全套重算數字；
   舊常數原文保留。

## 7. Artifact 案底（datestamped，處理歷史數據時必讀）

| 日期 | 症狀 | 成因 | 修法 |
|---|---|---|---|
| 2026-07-22 | 964 事件中 908 個「前向回報」實為追蹤首週行情 | 判定史始於 5 月、價格史始於 7 月，at_or_after 錯配 | 規則 4.2（7 日對齊），錯配事件前向回報清空 |
| 2026-07-22 | 34 條 leg 回報恆為 0 | 07-22 補行未 sync 價（snapshot_price_date 未變） | 規則 4.3（stale-leg），`cmd_snapshot` 加寫行警示 |
| 2026-07-21 | 首版「首個訊號」表（降級 −1.5%、等級單調）誤讀為訊號 | 上述兩 artifact ＋事件重覆計數 | RELEASES.md 已附正式更正；主口徑改 cluster×超額 |
| 2026-07-22 | 07-18 週轉變量爆升至常態 4–6 倍（32 個 vs 平時 5–8）、方向偏淡（23 降 9 升）、跳≥2 級 12 個 | 判讀模型遷移（07-16 grok-4.5→deepseek-v3.2；07-18→v4-pro）——「換改卷老師」重新評分潮，非市場事件 | `model`／`model_transition` 欄入表；遷移日事件統計表剔除、觀察名單剔除；公開實驗延至 07-31 後首個 mark 開始；覆核計劃：追蹤 07-18 批轉變其後數週的反轉率——低反轉＝一次性口徑重校，高反轉＝模型噪音需整批標記 |
| 2026-07-22（續） | 凍結證據 A/B（12 樹×3 葉×3 reps，`_ab_results/model_ab_20260722_1805`）：deepseek-v4-pro 自我一致性 95%（同一證據可判出橫跨三級的不同答案）、無故改判 5 次中 4 次為降級、API 失敗 4 次；grok-4.3 一致性 100%、零失敗、更平（$0.038 vs $0.042）。deepseek 單次無故改判率 ~4.6% × ~610 葉 ≈ 每週 28 個純噪音轉變——與 07-18 實測 32 個吻合 | deepseek-v4-pro 判讀噪音（偏淡），非市場事件亦非口徑重校 | 艦隊 default 切換至 x-ai/grok-4.3（07-23 起）；07-16..18 全部非 recode 轉變由 `model_audit.py` 以 grok-4.3 零搜尋整體重審，更正以 recode:true 入帳；07-25（grok 首個週六判讀）預防性入 MODEL_TRANSITION_DATES |

| 2026-07-26（解除預防旗） | 2026-07-25 原列入 MODEL_TRANSITION_DATES，令當日 8 個判定轉變被排除於校準樣本與週名單之外 | 該旗於 2026-07-23 切換至 grok-4.3 時**預防性**加入——理由是「憂慮模型改變」，並非依據當日數據；其餘三日（07-16/17/18）皆有實證支持（07-18 轉變量 31 個＝常態 7.8 倍且方向極偏淡，另有凍結證據 A/B 佐證判讀噪音） | 維護者 2026-07-26 決定移除該預防旗：07-25 實測 8 個轉變（常態 2.0 倍）、方向均衡（4 升 4 降），不具重新評分潮特徵。移除後該日事件回復入校準樣本與週名單。**此為排除條件之放寬，於見數之後執行**——如實記錄，供外部按此重算或維持原排除 |
| 2026-07-26 | 判定追蹤訊息全部個股起點＝終點、回報一律 +0.0%、命中假報 0/7 與 0/12，並已推送至公開頻道 | 週日 cron 已寫入當週 mark 後，維護者為補發訊息而重跑同一 workflow，`append_nav` 無同日防呆，寫入第二行相同 mark；訊息以「最後兩行」界定計分窗口，兩行相同即窗口長度為零 | 刪除該重複行（同一 (date, kind) 之重複寫入屬重跑產物而非新事件，非改寫歷史）；`append_nav` 加同日同類防呆，已實測擋重複／放行新日期；誤導訊息以更正訊息追加，原訊息不刪 |
| 2026-07-25（GOOGL 換論點） | 校準事件總數 938→915——GOOGL 全部 31 個歷史事件退出資料集 | 維護者 2026-07-24 對 GOOGL 執行全樹 REPLACE（新論點：FCF 轉正 → EV/Backlog 重估），新假說檔 verdict_history 歸零——樹之重生等同舊論點退場 | 屬換論點之正常後果，非數據損失（git 歷史完整保留舊事件）；後續若換論點頻繁，需考慮退場事件封存機制（tombstone export），現階段記錄在案 |
| 2026-07-24（詞彙遷移） | 歷史升級事件 260 個中 223 個（86%）集中於 2026-05-12..16 一週 | 初版歸因「樹嬰兒期安定潮」（樹齡 ≤14 日規則），維護者質疑後以後期 cohort 對照**否證**：六月後新建 16 棵樹首 14 日僅 1.8 事件/棵、方向均衡——非樹齡效應。真正成因：05-12..15 艦隊由舊 3 值詞彙成批 remap 至 6 值制（455 個事件涉舊詞彙，390 個在 05-15），其後首個判讀週（05-16）大規模重新評分；05-12..16 潮內 245 個升降級**全數**落於「該樹遷移日 +7 日」窗口 | events.csv 加 `vocab_migration` 旗（from/to 涉舊詞彙，或遷移日 −1..+7 日窗口）＋ `tree_age_days` 協變量；統計表剔除、原始行保留。樹齡規則廢棄——後期新樹的早期事件屬真訊號（如 MRVL 07-04 致命降級 +2 週 −23%），不得誤殺 |
| 2026-07-24（續） | 回補後 W27（07-04 週）事件超額全部精確 0、全池中位 0 | 首個實時行（07-09）帶「對上週末」舊價（07-04 標籤＝07-02 收市，07-03 為假期）——與 sidecar 末行揸住同一個收市價，兩行日期不同，產生 0% 假 leg 兼污染全池中位 | 邊界剪裁：sidecar 週六行日期 ≥ 首個實時行價格日 −1 日者剪走；leg 有效性收緊（規則 4.3 補充）：起訖兩行價格日相距 ≤1 日一律視同 stale，不出數（v3.2） |
| 2026-07-24 | 2026-03..07-04 之 886 個事件（41 檔）因無同期價格行而全部無法對齊——審計樣本僅剩 7 月 | 判定史始於 5 月、價格追蹤基建 7 月先上線 | `price_history_backfill.py`（手動 workflow）以 Yahoo 歷史日線回補該段每週六收市價，寫入 sidecar（`backfilled_hist` 標記，不入 quant_history）；事件帶 `price_era` 欄，分析全部分「回補段 vs 實時段」呈報（§8.6） |
| 2026-07-23 | 週報「無池內價格」／calib「價格對齊無效」比例偏高 | ①07-11 週六有 15/72 棵樹因個別 workflow 失敗整週冇快照行（星期五收市價缺失，無補數機制）；②07-09、07-22 兩次全艦隊中週補行未 sync 價（snapshot_price_date 停留上一週六）——stale 行夾在中間令 positional h1 leg 永久失效（受影響事件於 h2 位補見完整週回報）；③cmd_fetch_price 單源 Yahoo、無重試，失敗時 apply-patch 曾以空字串抹走舊 last_price_line | 價格 fetch 加重試 ×3 ＋ Finnhub 後備；apply-patch 禁以空 price 覆寫；新增週六 price-sweep workflow（只補價不觸判定）逐棵補齊過期收市價；歷史 stale 行保留原樣（stale 規則照剔），不回填 |

## 8. 特徵重要度工程（v3.0 預登記）

目的：以實現股價反應覆核我們**聲明**的重要度——某分支標成「極高」而市場
毫無反應，可能評錯；反之「極低」分支屢現強反應，可能低估。方法
取自量化業界與學術標準口徑，先寫規則、後見數據。

1. **訊號 IC 序列（Grinold–Kahn 口徑）**
   - 每週橫截面 Spearman rank IC：訊號＝cluster 之 `grade_weighted_delta`
     （等級加權淨變化），結果＝該 cluster 之 +1 週超額回報（`excess_h1`）；
     該週有效 aligned cluster ≥3 方計。
   - ICIR＝IC 序列均值 ÷ 標準差；≥0.5 為佳。
   - 誠實聲明（Fundamental Law：IR ≈ IC × √breadth）：本系統每週獨立
     事件僅約 5–10 個，breadth 有限——IC 再高，策略層 IR 亦有數學上限。
2. **事前窗口（分辨兩種「無反應」）**
   - calib 表新增 `pre_h1`／`pre_row_date`（對齊行**前一行**→對齊行之
     一週回報），分析層算 `excess_pre`（扣同期全池中位）。
   - 語義：事件後無反應＋事前亦無動靜＝「市場唔理」（等級候選可疑）；
     事件後無反應＋事前已大幅同向＝「市場先行」（消息早已入價，等級
     本身未必錯）。事件研究窗口口徑參照 MacKinlay (1997)。
3. **分支審計（兩層 empirical-Bayes shrinkage）**
   - 對每個（ticker×分支×等級）單元計降級事件之 `excess_h1` 原始均值，
     再作兩層信度混合（credibility／Efron–Morris 口徑，同 §6 之
     Z=m/(m+k) 式）：
     - 等級層：等級均值向全域降級均值收縮，z = m/(m+k_grade)，
       **k_grade = 30**（與 §6 重估門檻一致）；
     - 分支層：分支均值向所屬等級之收縮後均值收縮，z = n/(n+k_branch)，
       **k_branch = 10**。
   - 小樣本分支自動被拉向先驗——三兩個事件的「異常」不會被當成發現。
4. **旗標門檻（Harvey–Liu 多重檢定紀律）**
   - 只在 n ≥ 3 且 |t| ≥ 3 時上旗（t=3 對應 Harvey–Liu–Zhu 對因子研究
     多重檢定之建議門檻，遠嚴於慣用 t=2）：
     - ⚠️ 高重要度無負反應（降級候選）：極高／高而原始均值 > 0；
     - ⬆️ 低重要度強反應（升級候選）：低／極低而原始均值 < −3%。
   - 上旗且事前超額均值 < −2% 者加註「疑屬市場先行，非等級錯」。
5. **紀律：只 flag、不自動改**
   - 等級語義為「證偽時對論點的承諾後果」，屬樹作者的判斷承諾；旗標
     只提示重審，**任何等級改動須人手重答「證偽後跌到哪」**，並於該
     假說記 `grade_history`（舊等級、新等級、改動理由、觸發旗標）。
   - 常數層（IMPACT_GRADES 全域權重）之修改照走 §6 程序，兩者獨立。
6. **歷史價回補與年代分層（v3.1）**
   - 判定史始於 5 月、價格史始於 7 月——中間約兩個月的事件（~886 個）
     無法對齊。以 `price_history_backfill.py` 回補該段**每週六**收市價
     （Yahoo 6 個月歷史日線為主源，Finnhub candle 後備；該週 6 日內無
     收市價者如實跳過）。
   - 回補**不寫** `quant_history.jsonl`（該段無快照可言，捏造 state 行
     ＝改寫歷史）——寫入獨立 sidecar `{TICKER}/price_history_backfill.jsonl`
     （逐行 `backfilled_hist: true`＋`source`），僅供校準管道在該樹首個
     實時行之前合併。
   - 事件帶 `price_era` 欄（`hist_backfill`／`live`）；分析輸出必須分層
     呈報（回補段 vs 實時段）——兩段數據源**與判讀模型年代**（回補段屬
     grok-4.5 年代）都不同，兩段方向一致方可視為穩健。
7. **斷 state 比較（state 期間超額，非事件窗口）**
   - 每個假說的時間線按判定 state 切段（非 recode 轉變事件為界；首段＝
     首事件之 `from` state），逐段收集期間每週超額 leg（tree leg −
     同期全池中位）。
   - 去重單位＝ticker×週×state 分數（同一棵樹同週多片葉同 state 只計
     一次）；同一 ticker 同週可同時出現於不同 state 組（不同葉不同
     state），屬結構現實，如實保留。
   - 檢驗：週超額均值是否隨 state 轉差（Validated → Falsified）單調
     下行——即 state 唔只喺轉變一刻有訊息，期間都有。誠實聲明：樣本
     只含曾轉變的假說（存活偏差）；多假說共用同一價格序列，組間非獨立。
8. **後續層（已設計、未實施——實施前不得入正式輸出）**
   - 分支**類別**分類法（`branch_category` 枚舉：估值／需求／供應鏈／
     監管／技術路線／管治等）——全艦隊分類工序完成後，審計加類別層，
     直接檢驗「某一類分支權重已經唔同咗」；
   - MDA（mean decrease accuracy）meta-model 特徵重要度，配 purged
     cross-validation 防洩漏（López de Prado 口徑）；
   - 市場模型 beta 敏感度（現行超額口徑為對池中位，未調 beta）。

文獻依據：Grinold & Kahn《Active Portfolio Management》（IC／ICIR／
Fundamental Law）；Harvey, Liu & Zhu (2016)〈…and the Cross-Section of
Expected Returns〉（t≥3 門檻）；Efron & Morris (1975)（empirical Bayes
shrinkage）；MacKinlay (1997)（事件研究窗口）；López de Prado《Advances
in Financial Machine Learning》（MDA、purged CV）。

## 9. 版本紀錄

- **v3.14（2026-07-27）**：證據與判定分離（Pass 3.5）＋改判獨立查核者。
  維護者問「點樣杜絕呢件事再發生——重寫 prompt？分步驟？分 agent？」。診斷
  結論：不是 prompt 問題——已有的 MUST 級指令（引用須來自證據池、證偽判定
  須引用真實條件、breached 條件不得跳過）全部曾被違反；結構問題在於 Pass 4
  一個 call 同時寫證據與落判定，斷言與查核是同一個動作、同一個 actor。
  1. **Pass 3.5 證據對映（分步驟）**。判定之前先由獨立 call 把本週搜索結果
     對映成證據行並派發本週 eid（W{日期}-{序號}）；該 call 不被告知現行
     判定、不被問任何判定問題。source_url 必須逐字存在於本週搜索池，否則
     該行以確定性檢查丟棄——對映層自己杜撰來源也攔得住。
  2. **證據凍結**。Pass 4 收到的帳本已含 W 行；提示明示「帳本即引用宇宙的
     全部，supporting／contradictory 僅作展示，不會入帳」。落判定時證據集
     已凍結，「作一條證據來支持結論」在結構上做不到——CEG E004（遷移程序
     自寫證據）與 IFNNY C3（引用不存在的 Gartner 報告）皆屬此類。
  3. **寫入路徑換源**。入帳（recent_evidence）的證據改由 Pass 3.5 供給；
     Pass 4 的 sup／con 僅供 Rule 1 比對。「被引用的 URL」與「入帳的證據」
     首次來自兩個不同的 call，Rule 1 的循環論證（兩者同源、交集必然成立）
     隨之消解。W-eid 經 apply-patch 保留至 recent_evidence（weekly_helpers
     74 份同步），driver_eids 的引用日後可解析。
  4. **改判獨立查核者（分 agent）**。只在改判時跑（全艦隊每週約 8 個）。
     查核者收到假說原文、條件現況、被引 eid 的證據文本——刻意不給判讀的
     reason，否則查核的是散文的流暢度而非證據本身。查核不通過即 drop
     （dropped_reason=verifier_rejected，連同評語入 run log）；查核 call
     故障則 fail-open 放行——它是輔助檢查，API 故障不應令管道停擺，硬規則
     仍在 Rule 1–3。
  回退開關：TWOPASS_SPLIT_EVIDENCE=0 停用 1–3、VERDICT_VERIFIER=0 停用 4。
  首個週六（2026-08-01）判讀為實戰首跑，維護者宜檢視 run log 的
  「Pass 3.5 mapped」「verify-verdict-changes」兩段。

- **v3.13（2026-07-27）**：E2 對稱化，E5 存量送回判讀而非批量改寫。
  1. **E2 擴至 `met`**。對一條描述失敗情境的條件而言，`met` 與 `breached`
     同屬壞消息。CEG C2-F1（`kind=deadline`，文本為「2026 Q3 前無新 PPA 公告，
     且 Q2 財報管理層指引無具體合約時間線」）status 為 `met`，即該失敗情境
     已成立，判定卻升至 Validated；E2 原本只認 `breached`／
     `expired_unfulfilled`，於是漏過。只對 falsification／deadline 兩類套用
     ——verification 條件的 `met` 是好消息。新觸發三片：CEG C2、ONDS A1、
     PLTR C2。`--check` 失敗為 non-blocking（automated_helpers 明示），只令
     fleet-audit 標紅並落 run log，不阻每週判讀。
  2. **E5 存量（24 片葉）不作批量改寫**。「判定落證偽區而無條件觸發」有兩
     個可能：判定站不住腳，或條件紀錄漏了更新。兩者都是關於該假說實質的
     裁決，須由掌握證據的判讀作出。由本專案批量改寫以令檢查通過，正是
     2026-07-11 遷移程序為了把 CEG C2-F1 標為 `met` 而自行捏造一條證據的
     成因——同一個失敗模式不應為了清 backlog 而重演。
     改為在判讀提示中加入 E5 告示：該葉進入判讀時會被明確告知其判定與條件
     紀錄不符，必須二擇其一並在理由中交代——(a) 某條條件確已滿足，指明 cid
     並引用滿足它的數字／事件，同時在 condition_assessments 標 met；或
     (b) 沒有條件滿足，則證偽區判定缺乏預登記條件支撐，除非本週證據獨立
     支持，否則應升出該區。提示明文禁止為了成立 (a) 而杜撰證據。
     存量因此會在其後各次判讀中逐片消化，每一次都有證據與理由在案。

- **v3.12（2026-07-27）**：判定改變的**出處**（provenance）納入紀錄，並補上
  §8 E5 閘。緣起：NET/A3 於 07-25 由「接近證偽」改判「已證偽」，理由引用
  Google 於 2026 年 4 月推出 Agent Gateway——而同一件事實已於 06-06 用作上
  一次降級的理由，其間並無新資料入帳。該葉自身的 `conditions[A3-F1].status`
  為 `open`、`condition_assessments` 為 `approaching`，與 Falsified 的判定
  互相矛盾，而現行 `--check` 回 OK。

  **既有機制（本次確認仍然有效，不重複建造）**
  - `cmd_sweep_conditions`：零-LLM 確定性閂鎖，週六判讀前跑。期限到期 flip
    `expired_unfulfilled`、scalar 條件穿閾 latch `breached`，兩者皆注入一條
    `tier: synthetic` 證據並以 `bears_on` 指向該條件。「承諾的事沒有發生」
    本身就是證據——此設計早已到位。
  - `_validate_verdict_changes` 三條硬規則：Rule 1 要求引用本週證據 URL、
    Rule 2 要求證偽類判定引用真實證偽條件、Rule 3 擋純股價理由。
  - §8 E1–E4 閘，其中 E2 為 anti-goalpost（條件已觸發而判定仍正面即 error）。

  **兩個缺口**
  1. **Rule 1 量錯了「新」**。它只問引用的 URL 有沒有出現在本週抓回的證據
     池。常青文件頁（如 `docs.cloud.google.com/…/agent-gateway-overview`）
     每星期搜都會回同一條，永遠落入本週池；於是六月已用過的事實，七月再
     引用一次照樣過關。真正該問的是「引用的是不是本葉帳本未曾收錄的」。
  2. **E 閘單向**。E2 防「條件觸發而判定死撐正面」；反方向——判定跌進證偽
     區而無任何條件觸發——無閘。全庫 24 片葉處於此狀態。

  **本次新增**
  - **出處分類**（`_validate_verdict_changes`）：對照本葉 `evidence_ledger`
    已收錄的 URL 判定四類——`new_evidence`（引用了帳本未收錄者）／
    `stale_reread`（引用的全部早已在帳本內）／`expiry`（僅由 synthetic 閂鎖
    驅動）／`none`（引用不到任何東西）。寫入 `verdict_history.provenance`
    與 `driver_urls`（`weekly_helpers` 逐檔 73 份同步）。
  - **不阻擋，只標記**。無新證據的改判正是要研究的對象，drop 掉等於銷毀樣本，
    之後永遠答不到「無證據驅動的改判預測力是否較差」。處置在建籃層做
    （`_paper/basket_exclusions.json`），記錄與處置分開。
  - **§8 E5**：判定落證偽區、而無任何證偽／期限條件為 `met`／`breached`／
    `expired_unfulfilled` 且無一條評為 `met` → 警告。暫列 warnings 而非
    errors：全庫現有 24 片葉違反，即時升為 error 會令 fleet-audit 與每週逐檔
    判讀的 `--check` 一併失敗，把清存量的成本壓在每次跑批上。存量清理後改為
    error。

  **可研究的問題（本次之後方能回答）**：`provenance` 分類與其後一週超額之
  關係——`new_evidence` 驅動的改判，預測力是否高於 `stale_reread` 與
  `expiry`？`tier` 早已分開（news 3257／earnings 661／filing 297／
  trade_press 161／synthetic 8），缺的只是判定與證據之間那條連結，本次補上。

- **v3.11（2026-07-27）**：計分板新增**累計分層**讀數，並就兩條假說作
  前瞻預登記。緣起：2026-07-27 的回溯研究（見 `HORIZON_STUDY_20260727.md`）
  顯示整體訊號分不到 H1／H2，但拆開之後有兩個形態值得前瞻檢驗。回溯結果
  一律**不入帳本**——帳本是預登記記錄，倒填等同事後造數；故累計行自
  2026-07-26 起前瞻累積，回溯數字只存於該研究文件。

  **H-grade（本條為前瞻預登記）**
  - 假說：降級之衝擊等級愈重，其後一週超額愈負。
  - 回溯觀測（35 個叢集、9 個判讀週，剔除流程性改判後）：致命級 h1 命中
    7/7、均值 −6.0pp，散佈 6 個判讀週 6 隻股票，逐週留一後每一變體仍為
    6/6 或 5/5；其餘等級合計 5/13、+2.2pp。等級權重與 h1 超額之相關
    r=−0.41。單尾二項 p=0.0078。
  - 需留意：重創級（次重）h1 為 +5.4pp、方向相反，故並非單調關係，實為
    「致命級 vs 其餘」之二分；升級側無鏡像（致命級升級 h1 僅 2/5）。
  - **判準（前瞻，先此聲明）**：累積至 15 個致命級降級叢集後，若 h1 命中
    率 ≥70%、且逐週留一之最差變體仍 ≥60%，方檢討「只做致命／重創級」之
    入籃規則。未達標則維持一視同仁。

  **H-priced-in 之兩側不對稱（本條為前瞻預登記）**
  - 假說：事前已朝訊號方向走過者，其後表現較差（已被市場計入）；且此效應
    在升級側強於降級側。
  - 回溯觀測：事前走勢與 h1 超額之相關，升級側 r=−0.60、降級側 r=−0.06；
    降級側要到 h3 才出現（−0.42）。升級側「事前已升 >5%」一格 h2／h3 命中
    0/4、均值 −12.7／−16.6pp。
  - 需留意：該格 n=4，其中 3 筆來自 2026-05-30 同一週，且含 ONDS（事前
    +45.9%）、SNOW（+48.4%）等已爆動能股。方向可信，幅度不可信。
  - **判準（前瞻）**：升級側「事前已升 >5%」累積至 12 筆後，若命中率
    ≤35%，則將「事前已反應」gate（§5.6 行動鏈第 3 項）由僅降級側擴至
    升級側。

  實作：`_review_block` 逐筆記錄（等級、事前走勢、超額、命中）入
  `review.legs`；`_strata_lines` 每週重算整段歷史並印出。存原始欄位而非
  已分層之結果——分層界線日後可能重估，原始欄位可重算。過渡期（名單早於
  `EXPERIMENT_START`）同樣記錄：分層讀數屬研究性觀測，與「累積計分板」
  這個預登記比分是兩件事，不因名單早於起始日而失效。

- **v3.10（2026-07-27）**：計分板控制後讀數之修訂，源於對 v3.9 實作的
  對抗式覆核（兩輪，第二輪五條全數成立）。五項均屬呈報或口徑層面，
  不改判定引擎、不改主讀數之定義：
  1. **分層變量改回超額**。`_pre_rets` 原以**原始**一週回報分層，與本節
     §5.6 預登記之 `excess_pre`（扣同期全池中位）不符，且原始碼註解誤稱
     「對齊 calib_analysis.PRE_BUCKETS」。已新增 `_pre_excess`，中位一律
     取自全池，兩個呼叫端共用同一中位。影響：同一條 −8 界線之下分層人數
     改變（2026-07-26 該批由 5 升 5 降變為 3 升 3 降），公開數字隨之更新。
  2. **對照組讀數統一為損益方向**。沽空側原印「被降級股比對照組多賺多少」
     ——對沽空倉而言是蝕卻顯示為正數，與同一則訊息內「沽空 6/12」及明細
     標題「沽空側」（皆愈高愈好）方向相反。現沽空側取負號，全訊息所有 pp
     一律「正數＝該邊賺錢」。
  3. **對照組定義收緊並如實描述**。原只排除名單週改判者，令量度週之內改判
     的股票留在對照池，而腳註卻寫「本週無判定改變者」。現同時排除名單週與
     量度週之改判者，腳註據實表述。腳註亦改報**實際用到**的對照檔數（跳過
     的格從未參與比較），並顯示本側檔數。
  4. **本側樣本下限**。CTRL_MIN_N 原只約束對照格，本側格 n=1 亦照發表。
     現對「該側合計」設同一下限；逐格不設限——逐格剔走會系統性排除某類
     事前走勢的股票而令估計量有偏，卡總數則純屬呈報門檻。
  5. **控制後讀數入帳**。兩個讀數原本只存在於 Slack 訊息，無從重算覆核。
     現寫入 `verdict_watch.jsonl` 的 `review.ctrl`（逐側 pp／本側數／對照數）
     與 `review.ctl_pp`，與「先公開、後對答案、可追溯」之承諾一致。
  另記：`_control_lines`（對照組）此前未在本規格出現，本條為其首次登記；
  `_controlled_line`（控制事前走勢後）於 v3.9 ③ 已有登記，維持不變。
  實作層另修：名單帳本兩個寫入點改為同日 upsert（原為無條件 append，重跑
  會令該週每個數字在下游重複計算）；`_pre_rets` 逐行防護（本函式現掃全池，
  任何一棵樹一行壞資料原會令整則訊息連同帳本寫入一併消失）。

- **v3.9（2026-07-27）**：H-priced-in 行動鏈由連續阻尼改為**二元 gate**，
  分桶檢驗擴至**兩個方向**，並新增「控制事前走勢後」之並排讀數。
  觸發：2026-07-26 名單之實測——降級組事前一週跌幅與其後計分週回報相關
  係數 −0.52；升級組事前平均 −11.0%（比降級組 −5.9% 更殘）、其後平均
  +9.9%。同一個反轉效應同時抬高買入側、壓低沽空側，故 v3.8 只檢降級側
  會把市場反轉誤讀為「降級訊號較弱」。策略回報未控制 +13.0pp、控制後
  +9.2pp——效應未消失但約三成來自反轉。
  ①分桶表兩側皆出（calib_analysis.priced_in_section）。
  ②λ(pre) 連續阻尼**撤回**：需估計門檻與折扣率兩個參數，單週約 20 個樣本
  估不出；改為單一預登記門檻之 gate——|事前超額| 超過門檻且與訊號同向者
  標記「已反應」，如實入帳但不入籃。失敗後果由「注碼算錯」變為「不入籃」。
  門檻待累積 ≥30 個獨立樣本後由分桶表估計，估定前 gate 不生效。
  ③計分板並排報「控制事前走勢後」之策略回報（分兩層、層內相減、按樣本數
  加權）；主讀數不變。
  ④新增訊號延遲量度（證據日→判定日，上限代理）：延遲短者屬「與價格同週」、
  長者屬「賭市場數週未反應」，先驗不同，v3.8 之前混為一談。
- **v3.8（2026-07-25）**：①訊號分同葉同週淨額化（§0）——一葉一週
  一票，首 from→末 to 淨方向，淨零不計；07-19 週 TSM C3 連降兩級被
  重複計數（−10.0→−7.5）、UNH B1 連升同病（+10.9→+8.4）為觸發案例；
  weekly_mark 與 dashboard 同步。②§5.6 增補 H-priced-in（市場先行
  阻尼）預登記——事前一週已沿訊號方向大幅移動之訊號屬追認，預期命中
  率較弱；07-19 週 4/4 反例為啟發樣本（in-sample），門檻由校準數據
  分桶檢驗後定，λ(pre) 流程同 H-age。分析層加事前分桶表。
- **v3.7（2026-07-25）**：Pass-2 query planner 開啟 §8 條件帳摘要
  （PASS2_CONDITIONS_DIGEST=1，全艦隊＋財報 cron＋新樹模板）——A/B 依
  據：覆蓋率 59%→76%（12 葉），判定質量三關 PASS（一致性 0.958 vs
  1.0、方向偏移 0.083、成本 +4%；exp C，活躍葉 12 片）。此為判讀輸入
  變更：分析 07-26 後轉變率如見異動，先回查此日。
- **v3.6（2026-07-24）**：訊號樹改裝第 1 期（維護者核准之四期路線）——
  葉子普查工具 `leaf_census.py`：機驗（machine）／判讀（judge）／未遷移
  （unmigrated）**驗證通道**分類（維護者釐清：非品質等級——判讀葉係
  系統主引擎；實證：2081 個 open 條件僅 5 個曾被機器 latch，99%+ 判定
  轉變不論葉類皆由 judge 產生，判讀葉校準命中率與機驗葉相當）；純
  metadata、不改判讀；報告 `LEAF_CENSUS_20260724.md`（910 葉：機驗 65%
  ／判讀 28%／未遷移 6%；GOOGL／ON／RDDT／SKHY 四樹未遷移 §8）＋校準
  事件按驗證通道分解表。setup skill v4.18 加 Observable Leaf Admission
  Test（8 問、`leaf_nature` 欄；紅線僅三類：price-derived、無方向無
  invalidation、因果鏈重複計票）。第 2 期（schema 加欄＋未遷移樹補
  §8）、第 3 期（node_logic；judge 葉降權僅在分解表出現系統性差距時
  啟動，需 A/B）、第 4 期（三層 target 驗證）候。
- **v3.5（2026-07-24）**：§5.6 增補 H-age（論點時效，維護者提出）——
  樹的論點寫於建樹一刻，訊號強度可能隨樹齡改變；`tree_age_days` 分桶
  監察表（≤30／31–60／>60 日，state 持有照剔，cluster 計數）入分析
  輸出；雙尾預登記、首讀門檻每桶 ≥10 個獨立 cluster；識別警示（樹齡
  與日曆週現階段共線）成文；行動鏈＝確認後方可加 λ(age) 衰減因子，
  函數形式先預登記。第二層（條件時效：§8 conditions deadline 距離）
  列為後續。
- **v3.4（2026-07-24）**：state 持有規則由「已記錄、供剔除」改為**強制
  執行**（維護者查 ONDS 案例時發現分析層從未落實）——事件層：+h 週格
  之窗口跨越同一假說下一次轉變（`next_transition_days` < 7h）即剔，
  適用於方向曲線、等級曲線、年代分層、分支審計；cluster 層：clusters.csv
  加 `next_state_change_days`（該 ticker 下一次升／降級事件距 cluster
  最後事件日數，不論任何旗——判定變了 state 就變），純度表以此剔格。
  效果：成熟期純降級週訊號加權超額由 +1週 −3.3% 隨窗口走闊至 +4週
  −7.9%（真 state-to-state 窗口）；純升級週跑輸池並非跨 state artifact
  （僅 1 個 cluster 受影響），如實呈報。
- **v3.3（2026-07-24）**：詞彙遷移旗——events.csv 加 `vocab_migration`
  （from/to 涉舊 3 值詞彙，或落於該樹遷移日 −1..+7 日窗口）＋
  `tree_age_days` 協變量；統計表剔除、原始行保留。初版曾以樹齡 ≤14 日
  定義（setup_wave），維護者以後期 cohort 對照否證（六月後新建樹首
  14 日僅 1.8 事件/棵、方向均衡——非樹齡效應，係 05-12..15 詞彙 remap
  一次性系統事件）後改為遷移窗口定義，樹齡規則廢棄。§7 記案底。
- **v3.2（2026-07-24）**：邊界剪裁與 leg 有效性收緊——sidecar 週六行
  日期 ≥ 首個實時行價格日 −1 日者於合併時剪走（首個實時行常帶對上
  週末舊價，同一收市價掛兩行）；起訖兩行價格日相距 ≤1 日之 leg 一律
  視同 stale 不出數。§7 記案底（回補後 W27 全零 artifact）。
- **v3.1（2026-07-24）**：歷史價回補 sidecar＋年代分層＋斷 state 比較
  （§8.6–8.7）——5–6 月約 886 個無法對齊事件以 Yahoo 歷史週線補齊對齊
  基準（sidecar，不寫 quant_history）；事件加 `price_era` 欄，全部統計
  分「回補段 vs 實時段」呈報；新增 state 期間每週超額對照表（轉變事件
  切段，ticker×週×state 去重）。§7 記案底。
- **v3.0（2026-07-24）**：新增 §8 特徵重要度工程——每週 rank IC＋ICIR
  （Grinold–Kahn 口徑）、事前窗口欄（`pre_h1`／`pre_row_date`／
  `excess_pre`，分辨「市場唔理」與「市場先行」）、分支審計兩層
  empirical-Bayes shrinkage（k_branch=10／k_grade=30）、Harvey–Liu
  t≥3 旗標門檻。只 flag 不自動改：等級改動須人手重答證偽後果並記
  `grade_history`。branch_category 分類層與 MDA meta-model（purged CV）
  列為後續層。原 §8 版本紀錄順延為 §9。
- **v2.10（2026-07-24）**：匯報／名單發佈日由週六改**週日**（樹照週六
  判讀；週六＋週日兩輪價格掃底完成後，週日 16:00 HKT 發佈——價格覆蓋
  最齊，且名單落在週一 sleeve 執行前一日）。首份正式名單 08-02、首次
  計分 08-09；`trigger=weekly` 之語義不變（週六掃描）。頻道分工同日
  生效：portfolio＝Kelly；long-short＝判定追蹤＋長短倉；option＝期權。
- **v2.9（2026-07-23）**：H-trigger 財報層操作規則——事件當日即時公開至
  實驗頻道（`_earnings_cron` 自動）、計分基準＝事件日收盤（週報名單記
  `earn_base`，對答案由基準價開錶）、照隨週日名單落賬分層呈報。
- **v2.8（2026-07-23）**：point-in-time 價格回補工具（`price_backfill.py`
  ＋手動 workflow）——以 Yahoo 歷史日線補返 07-11 缺行的 12 棵樹（另 3 棵
  無先前行、無狀態可搬，不補）。界線：只補客觀市場價與 carried state
  （該週無 judging 發生）、目標規則自動發現不容挑選、逐行帶
  `backfilled: true` 標記、pi_*／er_* 以舊 targets＋新價確定性重算。
  回補屬資料修復而非改寫歷史：價格可對照任一公開數據源覆核。
- **v2.7（2026-07-23）**：價格可用性加固（§7 末行）——fetch 重試＋Finnhub
  後備、apply-patch 空價保護、週六 price-sweep 掃底 workflow；目標：任何
  一棵樹不再出現「成週冇星期五收市價」。
- **v2.6（2026-07-23）**：§0 策略讀數由「組差」改為「策略回報」（缺邊
  以 0 計、全池對沖——單邊週照計分；雙邊週即組差）；新增持有期條款
  （持有至 state 翻轉或 H\* 上限，一週僅為最小追蹤單位）；H-spread 改
  策略回報口徑；review 欄新增 `strat_pp`／`wstrat_pp`。維護者裁定：
  「僅降級週無組差可計」屬錯誤框架——沽空降級對沖全池本身就是完整
  倉位，必須計分。
- **v2.5（2026-07-23）**：§5.6 增補 H-horizon（效應持續期：+1..+4 週累計
  超額持續走闊、持續期與 state 壽命相關；決定策略持倉期參數）；公開帳本
  README 主假說成績改三層結構（歷史資料集／先導逐週計分／正式實驗）。
- **v2.4（2026-07-23）**：events.csv 加 `trigger` 欄（weekly／earnings／
  空＝legacy）；判讀 patch 與 apply-patch 全艦隊 stamp 觸發源；§5.6 增補
  H-trigger（財報後裁定＝財報後漂移層，與週掃描分層呈報）；週報明細
  以 📅 標記財報後裁定、分維度摘要加按觸發源一行。
- **v2.3（2026-07-23）**：新增 §0 主假說與 Alpha 策略（組差／加權組差／
  訊號分定義）；§5.6 增補 H-spread（主策略讀數）；週報第二條 message
  改組差頭條＋合併排榜，`verdict_watch.jsonl` review 欄新增
  `spread_pp`／`wspread_pp`。
- **v2.2（2026-07-22）**：events.csv 加 `model`／`model_transition` 欄；
  遷移日事件於統計表與觀察名單剔除（原始行保留）；verdict_history 新
  entry 起帶 `model` stamp；公開實驗開始日定為 07-31 後首個 mark。
- **v2.1（2026-07-22）**：clusters.csv 加 `mix_type`（訊號純度）；新增
  H-purity 假說（附 in-sample 誠實聲明）與純度口徑 permutation 檢定。
- **v2（2026-07-22）**：加 event_id/cluster/pool/excess/necessity/state 壽命
  /conviction_delta 欄；clusters.csv＋廣度 cascade 層；分析腳本隨庫發布；
  預登記協議與重校程序成文。
- **v1（2026-07-21）**：首版 fwd_1w/fwd_4w 事件表（受 artifact 影響，見 §7）。

*研究用途，非投資建議。*
